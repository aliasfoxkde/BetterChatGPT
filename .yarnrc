# Yarn v1 configuration
# This ensures compatibility with the existing yarn.lock v1 format

# Use the npmjs registry
registry "https://registry.yarnpkg.com"

# Disable version check warnings
--install.check-files false
--install.frozen-lockfile true
